#!/bin/bash

#while IFS='' read -r line || [[ -n "$line" ]]; do
#    apm update $line
#done < "atom-packages"

while IFS='' read -r line || [[ -n "$line" ]]; do
    sudo zypper in $line
done < "system-packages"
